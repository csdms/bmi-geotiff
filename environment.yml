# An environment to develop, build, test, and document bmi-geotiff.
name: geotiff
channels:
  - conda-forge
dependencies:
  - python =3
  - pip
  - pyyaml
  - rioxarray
  - rasterio >=1.0
  - bmipy
  - make
  - black
  - flake8
  - isort >=5
  - pytest
  - pytest-cov
  - pytest-datadir
  - coveralls
  - bmi-tester
  - sphinx
  - recommonmark
  - pandoc
  - notebook
  - matplotlib
  - cartopy
  - build
  - pip:
    - zest.releaser
